import { PrintTitleDirectiveDirective } from './print-title-directive.directive';

describe('PrintTitleDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new PrintTitleDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
